AC_INIT(t1ascii.c)

VERSION=1.5
AC_SUBST(VERSION)

AC_PROG_CC
AC_PROG_INSTALL
AC_C_CONST

AC_ARG_ENABLE(warnings,
[  --enable-warnings       compile with -Wall],
if test "$enableval" = yes ; then
  CFLAGS="$CFLAGS -Wall"
fi)

AC_OUTPUT(Makefile)
