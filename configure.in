AC_INIT(t1ascii.c)
AM_INIT_AUTOMAKE(t1utils, 1.7.1)

AC_PROG_CC
AC_C_CONST

AC_PROG_INSTALL
AC_ARG_PROGRAM

AC_ARG_ENABLE(warnings,
[  --enable-warnings       compile with -Wall],
if test "$enableval" = yes ; then
  CFLAGS="$CFLAGS -Wall"
fi)

AC_OUTPUT(Makefile)
